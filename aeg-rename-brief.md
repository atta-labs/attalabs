7. **Regenerate the docs index (MANDATORY final step before committing).** Run the repo's docs-index generator so `docs-index.md` reflects the new `aeg-root/` + `aeg-project/` paths and the `projects.md` rename. The command is the repo's `docs:index` script — run **`bun docs:index`** (or, if that's not the script name, inspect `package.json` for the `docs:index` / index-generating script and run it). This MUST run after all moves and edits (steps 1–6) and before the commit, so the regenerated `docs-index.md` is part of the same single commit. If the generator genuinely doesn't exist or errors, hand-edit every `project-management/` and `products.md` reference in `docs-index.md` to the new paths instead, and say so explicitly in the report. Do not commit with a stale `docs-index.md`.
8. **Verify** (see §7), then **one commit** (see §8).