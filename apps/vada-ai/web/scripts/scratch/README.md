# Scratch scripts

Throwaway investigation scripts. This folder is gitignored.

Use for one-off work: bench audits, API probes, DB queries, migration rehearsals.

If a script becomes permanent (runs regularly, referenced from docs), move it to `scripts/` proper and commit.
