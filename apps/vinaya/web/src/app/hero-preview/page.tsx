import { VinayaHeroEmblem } from '../(site)/_components/hero-canvas/VinayaHeroEmblem'

// SCRATCH ROUTE — motion-design sandbox for the Vinaya hero emblem (Issue #602 part 1).
// Delete before the PR; the emblem gets swapped into HeroSection once the motion is right.
export default function HeroPreviewPage() {
  return <VinayaHeroEmblem />
}
