## June 23, 2026 — Herald task 5: report quality — evidence-tiered prompt + fixture evidence

### Herald

- **Task 5 (#171, PR #193)** — Improved the `SkepticalAuditor` system prompt in `packages/agents/forensic-hiring-auditor/yamls/herald-auditor.yaml` with five concrete instruction changes synthesized from forensic hiring framework research: (1) evidence tiering — every signal classified High/Medium/Low based on independent verifiability (GitHub structural > specific metrics > bare listings); (2) recency weighting — recent evidence (last 2 years) outweighs older claims; (3) signal scoring rules — 4–8 signals required, one per distinct pattern, bare-listing-only technologies go to gaps not signals; (4) quantitative grade thresholds (A: 6+ signals / 4+ High; A-: 5+ / 3+ High; B+: 4+ / 2+ High; B: 3+ / 1+ High); (5) interview hook rules — 3–5 required, each anchored to a named artifact, each probing a decision boundary. Added `tests/fixtures/` with before/after evidence and scoring.json (4/12 → 12/12). 14 regression guard tests in `tests/prompt-quality.test.ts` run without LLM call. Prompt-only change — no MatchReport schema, NO-FIT gate, UI, or adapter changes. Tier 1.
