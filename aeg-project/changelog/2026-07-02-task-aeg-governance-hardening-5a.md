## July 2, 2026 — Branch-ID verification: Step 0 must literal-match the topology's `#` column (D-073)

### AEG
- **aeg-governance-hardening task 5a (#293)** — Closes a confirmed, 100%-prevalence governance gap discovered while dispatching `vada-production-v1`'s benchmark-harness wave: every numbered task branch in that iteration (tasks 1–9) used a `T`-prefix (`T1`, `T2`, …) against a topology `#` column that is always a bare ID (`1`, `2`, …). The mismatch went undetected for over a week — tasks 1–7 merged before `forge-lifecycle.yml`'s `closes-n-gate` (D-069, added 2026-07-01) existed; tasks 8 and 9 (#289, #284) are the first two branches in the iteration's history ever evaluated by that live gate, and both failed on first inspection.
- **Two independent, substance-identical checks added.** `aeg-root/skills/brief-authoring/SKILL.md` §5 now requires the Brief Author to read the task's topology row and confirm the Step 0 branch-name suffix literal-matches the `#` column, character for character, before writing it into the brief. `aeg-root/roles/developer.md` entry-gate item 6 requires the Developer to independently re-run the identical check before executing Step 0 — never trusting the brief was authored correctly. Both refuse (STOP language, not advisory) on mismatch.
- **Contract tightened, not extended.** `aeg-root/contracts/brief-developer.md`'s "Worktree step 0" field-table row and the Producer/Consumer-obligations bullets are tightened to name this obligation explicitly. This is not a new hand-off field — it's a shared precondition both sides independently verify against the same third source (the topology file), the same shape as the existing Issue-existence precondition (D-054).
- **Ratifies D-073** (Type 2, Lock: YES — a load-bearing dispatch-safety rule; changing or weakening either check requires a superseding decision).
- **Deliberately out of scope.** The 9 historical mismatched branches (`T1`–`T7` merged/frozen; `T8`/`T9` already corrected live pre-dispatch as `task/vada-production-v1/8` and `/9`, PRs #295/#286) are not retroactively touched. `packages/aeg-core/bin/verify-coherence.ts` and the `closes-n-gate` CI job are untouched — this task fixes the upstream process that feeds that gate, not the gate itself.
- **Surface:** `aeg-root/skills/brief-authoring/SKILL.md` (§5), `aeg-root/roles/developer.md` (entry gate item 6 + summary line), `aeg-root/contracts/brief-developer.md` (field-table row + two obligations bullets), `aeg-project/decisions.md` (new D-073 entry). No code, no `packages/**`, no `.github/**` touched. Closes #293.
