## June 24, 2026 — Vāda T3a: equip reviewers with vendor-native web search

### Vāda / Adapter

- **T3a (#178, PR #205)** — Equips the three non-Anthropic Vāda reviewer agents (Gemini, GPT, Grok) with `tools: [web_search]` declared in YAML — YAML is the declarative source of truth, no hardcoded tool logic in the route. New `webSearchHandler` (`packages/adapter-langgraph/src/web-search-handler.ts`) for OpenAI-compat vendors: resolves Google CSE (`GOOGLE_SEARCH_API_KEY` + `GOOGLE_SEARCH_CX`) → Tavily (`TAVILY_API_KEY`) → graceful empty fallback. Handler wired via existing `LangGraphAdapter.customTools` parameter (T3 infrastructure, no new route-level abstraction). Google (Gemini) routes automatically to native `googleSearch` grounding via `GOOGLE_TOOL_REGISTRY`; Anthropic routes to server-side `web_search_20260209` — neither requires a handler. Docs updated: `atta-adapter-langgraph` skill (multi-vendor registries + `webSearchHandler`), `vada-yaml-authoring` skill (tool declaration guidance), `tools-capability-spike.md` (T3+T3a reconciliation), `vada-reviewers-spec.md` (fidelity-gap passages updated — web search gap closed for all three vendor types). Tier 1. Conforms-to D-053.
