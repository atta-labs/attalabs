## July 2, 2026 — Planner §7 auto-derivation from `doc-owners` (D-076)

### AEG
- **aeg-governance-hardening task 4 (#219)** — Closes the D-058-class leak where the Planner's "Docs to keep coherent" field and the Brief Author's §7 doc-update list were hand-curated from memory/reading, with no mechanical check against `aeg-root/doc-owners`. Now that task 3 (#218, PR #283) drove `doc-owners` to full bind-all coverage, §7 can be **derived**, not just remembered.
- **New pure helper.** `packages/aeg-core/src/derive-section7.ts` — `deriveSection7(intendedSurfaces, docOwnersContent)` matches a task's intended surface globs against `doc-owners` bindings via `globsOverlap` (segment-wise: `**` absorbs the remainder of either side, `*` matches exactly one segment, literals require exact equality), returning the deduped union of matched doc pointers plus per-match detail. Built entirely on the existing `parseDocOwners` — no second parser. Exported from `packages/aeg-core/src/index.ts`.
- **Role doc + contract tightened, not extended.** `aeg-root/roles/planner.md`'s "Docs to keep coherent" field description gains the derivation rule (Planner names intended surfaces; the Brief Author derives the floor at brief-authoring time against the live manifest). `aeg-root/contracts/planner-brief.md`'s corresponding contract row is tightened — same field, no new row, mirroring D-073's precedent.
- **`aeg-root/state-machine.md` §15 updated** — the "out of scope for D-062" list no longer names Planner §7 auto-derivation as backlog; it now points at D-076 and the new helper.
- **Ratifies D-076** (Type 2, Lock: YES — a load-bearing briefing-time obligation; weakening the override-with-reason rule or reverting §7 to pure hand-curation requires a superseding decision).
- **Deliberately out of scope.** The C5 doc-coverage *enforcement* gate at PR time (`evaluateC5`, `packages/aeg-core/bin/verify-docs.ts`) is untouched — this task makes §7 reproducible upstream of that gate, it does not change what the gate enforces. The helper is a Planner/Brief-Author aid only; it is not wired into any CI path. No resolved doc-pointer list is baked into an Issue body — the derivation runs at brief-authoring time against the live manifest.
- **Surface:** `packages/aeg-core/src/derive-section7.ts` (new), `packages/aeg-core/src/derive-section7.test.ts` (new), `packages/aeg-core/src/index.ts` (new exports), `aeg-root/roles/planner.md`, `aeg-root/contracts/planner-brief.md`, `aeg-root/state-machine.md` (§15), `aeg-project/decisions.md` (new D-076 entry). Closes #219.
