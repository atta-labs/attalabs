## July 2, 2026 — AEG Consolidation Task 2: verify-coherence checks homed in @atta/aeg-core

### AEG Core

- **Task 2 (#264)** — Relocates the 12 pure coherence-check evaluators (A1/A2/A3, T1/T2/T3, D1, L1/L2/L3, checkClosesN) plus 3 helpers (`isGrandfathered`, `COHERENCE_ENFORCED_FROM`, `resolveDepEntry`) and their 4 types (`CheckFailure`, `CheckResult`, `TaskEntry`, `IterationFile`) from `packages/aeg-core/bin/verify-coherence.ts` into a new pure module, `packages/aeg-core/src/coherence-checks.ts`, exported from `src/index.ts`. `bin/verify-coherence.ts` becomes a thin I/O shim: forge fetches (`fetchProvenance`, `fetchOpenIssuesByLabel`), filesystem reads (`loadIterationFiles`), the impure `checkN1N2M1M2M3` (delegates to Task 1's `checkDecisionNumbers`/`checkManifestValidity` — untouched), and CLI arg/format/exit handling all remain in `bin/`. Behavior is preserved byte-for-byte, including the existing (buggy) T3 grandfather branch-scoping proxy — confirmed via before/after golden-diff of `verify-coherence.ts --json` output (with and without `GH_TOKEN`) and a `--closes-n` smoke test, both identical. 56 new characterization tests in `src/coherence-checks.test.ts` (migrated + expanded from the deleted `bin/verify-coherence.test.ts`, plus new coverage for `checkClosesN`, which had none before) pin current behavior, including a test that documents the known T3 proxy bug rather than fixing it — the fix is Task 3's (#220) scope. Tier 3. Conforms-to D-070. Closes #264.
