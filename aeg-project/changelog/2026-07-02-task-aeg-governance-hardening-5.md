## July 2, 2026 — Ledger ownership, reviewer disk-isolation, executor-protocol chain (D-071)

### AEG
- **aeg-governance-hardening task 5 (#266)** — Encodes three live-fire lessons from dispatching D-069's own tasks into the AEG constitution. **Ledger ownership:** no role writes its own token-ledger row on a task branch anymore — chat/read-only roles (Reviewer, Security, Planner) and the Developer all **report** their tokens in the artifact their turn already produces (PR body "Token report" section for terminal roles, verdict comment for chat review roles, plan PR/report for the Planner); the per-task **Archivist** is now the sole writer of `aeg-root/iterations/<name>.tokens.md`, collecting every role's report and appending all rows — including its own — at close-out. Fixes the two live collisions: chat roles that structurally can't append (no branch, no filesystem access), and parallel Developer sessions racing on the same shared file (#255↔#258). **Reviewer disk-isolation:** two separate rules added to `reviewer.md`/`security.md` — write nothing to disk (verdicts are PR comments only), and dispatched agents run only in an isolated worktree, never the main checkout. **Executor-protocol chain:** `.claude/skills/executor-protocol/SKILL.md` now opens with a pointer to `aeg-root/roles/developer.md`, so a bare "read executor-protocol" dispatch still carries the Developer's entry gate and PR canonical form. **Ratifies D-071** (Type 2, Lock: NO — a future decision may move token capture into tooling).
- **Backlog pruned.** `specs/ecosystem-backlog.md`'s stale "grandfather L2/L3" punch-list line (superseded by merged #261) is deleted; the ledger-ownership, reviewer-isolation, and executor-chain items are struck through and annotated as dispatched by this task.
- **Deliberately deferred (flagged, not fixed).** `aeg-root/roles/team-leader.md` (Planner-mode/Brief-Author-mode duplicate turn-end text) and `aeg-root/roles/iteration-archivist.md` (its own `iteration-close` row) carry the same shape of self-append instruction but were **not** changed — both are iteration-wide chat-role turns with no task branch/PR to report into, which is the exact structural gap named in the brief's stop conditions ("the Archivist role doc has no clean place to record N roles reported tokens, no branch to append to"). Left for a follow-up task rather than inventing a recording mechanism ad hoc.
- **Surface:** `aeg-root/roles/{reviewer,security,planner,archivist,developer}.md` (turn-end sections + Reviewer/Security "what you do NOT do" additions + Archivist close-out checklist extension); `aeg-root/iterations/README.md` §12 (append-rule + two-capture-sources rewritten for post-merge appends; ledger format table unchanged); `.claude/skills/executor-protocol/SKILL.md` (chain line); `specs/ecosystem-backlog.md` (prune + annotate); `aeg-root/aeg-manual-flow.md` and `aeg-root/state-machine.md` (Part 4 coherence-sweep fixes — description-level self-append references aligned to match); `aeg-project/decisions.md` (new D-071 entry). No code, no `packages/**`, no `.github/**` touched. Closes #266.
