## July 3, 2026 — Pre-push guard: refuse pushes to a merged/closed-PR branch (D-069)

### AEG
- **aeg-governance-hardening task 18 (#335)** — Closes a live-fire incident class (2026-07-03): six topology rows landed on plan PR #325's branch *after* that PR merged, silently accepted by git/GitHub with no push-time signal, orphaning invisibly until AEG Studio and the coherence oracle disagreed about the iteration's state. `.husky/pre-push` now refuses any push to a `task/<iteration>/<n>` branch whose most recent pull request is already `MERGED` or `CLOSED`. Pure verdict logic lives in `checkDeadBranchPush` (`packages/aeg-core/src/dead-branch-push-guard.ts`), mirroring `classifyLeftover`'s no-`fs`/no-shell-out shape; the one batched forge call (`gh pr list --head <branch> --state all --json number,state --limit 1`) lives in a thin CLI shim, `packages/aeg-core/bin/check-push-target.ts`. **Deliberately fail-open:** any forge-unreachable outcome (auth, network, rate limit) maps to `UNKNOWN`, treated identically to "no PR yet" — a transient reachability issue must never block every push. Conforms-to D-069 (no new decision — this mechanizes an existing charter, not a new architectural choice).
- **Surface:** `packages/aeg-core/src/dead-branch-push-guard.ts` (+ test, 5 fixtures: `MERGED`/`CLOSED`/`OPEN`/`NONE`/`UNKNOWN`), `packages/aeg-core/bin/check-push-target.ts`, `packages/aeg-core/src/index.ts`, `.husky/pre-push`, `aeg-root/enforcement.md`. Closes #335.
