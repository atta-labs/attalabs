## June 16, 2026 — Verification phase + runtime Test Plan merge gate (task 10)

### AEG
- **Task 10 (#126)** — Adds the **Verification phase** to the AEG model as Phase 11, between Review (Phase 10, unchanged) and Merge (renumbered Phase 12). The eleven-phase flow is now twelve. The phase is **jointly satisfied by the Developer-agent and the Principal**, not a new role — it mirrors D-048's chat-vs-terminal token-capture asymmetry: the agent runs `[agent]` Test Plan items (non-auth, scriptable — SSRF rejections, parse checks, route responses, render smoke) against the booted app and posts the actual command output as evidence on the PR; the Principal runs `[principal]` items (auth-gated, key-dependent, visual — a signed-in BYOK audit, a ModelPicker render behind Clerk) in a real browser and ticks the checkbox. The asymmetry is structural — the agent cannot tick `[principal]` boxes and the Principal does not tick `[agent]` boxes — and is the whole shape of the gate. **`Test Plan: unit-tests-only`** is a first-class allowed value for pure-logic briefs (no API route, no page, no server action in §4); the CI unit-test gate is the whole proof. **Doctrine: CI green ≠ app boots ≠ feature works.** Surface (model-only, no app code): new `aeg-root/roles/verifier.md` (the phase's owner profile, the two halves, the merge gate, the entry gate, stop-and-escalate conditions, anti-patterns); new §9 "Test Plan" field in `aeg-root/skills/brief-authoring/SKILL.md` (subsequent sections renumber 9→10, 10→11, 11→12; the contract-conformance checklist adds the Test Plan gate; anti-patterns add the verification-related failure modes); `aeg-root/process.md` gains Phase 11 between Phase 10 and (renumbered) Phase 12, with the doctrine line at the top; `aeg-root/aeg-manual-flow.md` §5 run-order table adds steps 7a (agent half) and 7b (Principal half) plus §6 entry-gate paragraph (the `Pre-merge gate` subsection from PR #133 is preserved and re-keyed to the new Step 8 merge); `aeg-root/state-machine.md` §1 notes the phase-not-role distinction, §3 adds a "Test Plan execution" row to the mutation-permission matrix, §3 Reviewer-position line extends to include Phase 11, §12 trusted-discipline list adds the Verification phase. **Optional CI enforcer (`scripts/verify-test-plan.ts` + `.github/workflows/verify-test-plan.yml`)** is the checkbox-state parse companion — fails when any `- [ ]` box in the brief's Test Plan section is unticked; passes on `unit-tests-only`; passes with a warning when no Test Plan section exists (so existing in-flight PRs aren't broken). The workflow triggers on `edited` so PR-body ticks update the gate without forcing an empty commit. Per `state-machine.md` §12 the job runs but does not block merge until the Principal arms it in branch protection — same convention as `Conventions` and `verify-docs`. The doctrine ships regardless; enforcement strengthens incrementally on the same advisory → enforced gradient. **D-049 logged (Status ACTIVE, Type 1, Lock NO)**. The plan-commit Step 1 (adding Task 10 to the iteration topology) is the precursor PR #127, opened by the Planner; this PR (Steps 2–6) builds the implementation on top.
